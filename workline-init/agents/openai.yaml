interface:
  display_name: "Workline Init"
  short_description: "判断是否需要Workline并初始化长任务材料入口"
  default_prompt: "Use $workline-init to decide whether this needs Workline; only then create an active directory and preserve the rough requirement."
