interface:
  display_name: "Workline Run"
  short_description: "约束goal按tasks.csv执行任务并记录验证摘要"
  default_prompt: "Use $workline-run to verify all current Workline gates, execute tasks.csv by dependencies, and keep run.md updated."
