# PRD: {{title}}

## 背景与目标

## 典型流程

## 功能要求

每条功能要求使用 `### FR-<序号> <标题>` 三级小节，编号连续且不复用。
`tasks.csv` 的 `refs` 列引用这些编号，脚本据此回查是否有功能要求漏拆。
不要用项目符号代替 `### FR-<序号>` 标题，否则覆盖性检查不会运行。

### FR-1

## 非功能要求

需要单独拆任务的约束写成 `### NFR-<序号> <标题>`，并在任务 `refs` 中引用。
没有则写「无」，不要留空的 NFR 占位小节。

## 非目标

## 约束条件

## 验收标准

## 关键决策与澄清记录

| 时间 | 问题/决策 | 结论 | 来源 |
| --- | --- | --- | --- |

## 风险与待确认问题

| 问题 | 当前结论 | 是否阻塞任务拆分 |
| --- | --- | --- |
